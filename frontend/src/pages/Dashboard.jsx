const Dashboard = () => {
    return (
        <div className="container" style={{ padding: '4rem 0', textAlign: 'center' }}>
            <h1>Dashboard</h1>
            <p>Field owner dashboard for managing fields and bookings.</p>
        </div>
    );
};

export default Dashboard;
