$kind: http-request
name: Leave Team
method: POST
url: '{{base_url}}/api/teams/{{team_id}}/leave'
order: 15000
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 2xx", function () {
        pm.response.to.be.success;
      });
